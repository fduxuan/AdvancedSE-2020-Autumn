server:
  port: 10099
spring:
  application:
    name: sc-confirmfinish-service
  security:
    user:
      name: root
      password: root

eureka:
  client:
    service-url:
      defaultZone: http://root:root@sc:10086/eureka/
    instance:
      prefer-ip-address: true
management:
  security:
    enabled: false
